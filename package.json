{
  "name": "@genee/rest",
  "version": "1.3.0",
  "type": "module",
  "main": "dist/index.js",
  "browser": {
    "dist/index.js": "dist/index.browser.js"
  },
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "build:types:browser": "tsc src/index.browser.ts -t esnext --moduleResolution node --skipLibCheck --declaration true --emitDeclarationOnly --outDir dist",
    "build:types:node": "tsc src/index.ts -t esnext --moduleResolution node --skipLibCheck --declaration true --emitDeclarationOnly --outDir dist",
    "build": "node esbuild.config.js && pnpm build:types:node && pnpm build:types:browser"
  },
  "devDependencies": {
    "@genee/fetch": "^1.6.2",
    "@types/node": "^18.17.3",
    "esbuild": "^0.18.19",
    "typescript": "^5.1.6"
  },
  "peerDependencies": {
    "@genee/fetch": "^1.6.2"
  }
}